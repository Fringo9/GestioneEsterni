import { FilterQuarterPipe } from './filter-quarter.pipe';

describe('FilterQuarterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuarterPipe();
    expect(pipe).toBeTruthy();
  });
});
