import { PhysioFilterPipe } from './physio-filter.pipe';

describe('PhysioFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhysioFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
